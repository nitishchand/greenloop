{
  "name": "@{{PROJECT_NAME}}/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "expo-router/entry",
  "scripts": {
    "start": "EXPO_UNSTABLE_MCP_SERVER=1 expo start",
    "typecheck": "tsc --noEmit",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@nozbe/watermelondb": "^0.28.0",
    "@tanstack/react-query": "^5.0.0",
    "expo": "^54.0.0",
    "expo-router": "^6.0.0",
    "react": "*",
    "react-native": "*",
    "zustand": "^5.0.0",
    "@{{PROJECT_NAME}}/shared": "*"
  },
  "devDependencies": {
    "expo-mcp": "*",
    "jest": "^29.0.0",
    "jest-expo": "^54.0.0",
    "typescript": "^5.5.0",
    "@types/react": "*"
  },
  "jest": {
    "preset": "jest-expo"
  },
  "_bnb_note": "Versions are best-known at profile-authoring time; `npx expo install --fix` after the first install aligns react/react-native to the Expo SDK. Pinned for real by the MiniClinic demo (Plan 4)."
}
