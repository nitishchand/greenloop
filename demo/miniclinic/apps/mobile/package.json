{
  "name": "@miniclinic/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "expo-router/entry",
  "scripts": {
    "start": "EXPO_UNSTABLE_MCP_SERVER=1 expo start",
    "typecheck": "tsc --noEmit",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@nozbe/watermelondb": "^0.28.0",
    "@tanstack/react-query": "^5.0.0",
    "expo": "~54.0.0",
    "expo-router": "~6.0.0",
    "react": "19.1.0",
    "react-native": "~0.81.0",
    "zustand": "^5.0.0",
    "@miniclinic/shared": "*"
  },
  "devDependencies": {
    "expo-mcp": "*",
    "jest": "^29.0.0",
    "jest-expo": "~54.0.0",
    "typescript": "^5.5.0",
    "@types/react": "~19.1.0"
  },
  "jest": {
    "preset": "jest-expo"
  },
  "_greenloop_note": "react/react-native pinned to the SDK 54 pairing (RN 0.81 / React 19.1) — a floating react-native breaks the jest-expo preset (found by the first dogfood run). When bumping the SDK, re-pin via `npx expo install --fix`."
}
