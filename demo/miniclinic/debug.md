# miniclinic — Bug Ledger

> Numbered, append-only. Read this **before** debugging anything — if a symptom looks
> familiar, cite the entry ("see Bug 8") instead of rediscovering it. Append a new entry
> after every root-cause fix; a fix without a Lesson line is not done.

## Bug 1 — {{one-line symptom}}

- **Symptom:** {{what was observed, including the exact error/failing tail}}
- **Root cause:** {{what was actually wrong — evidence, not guesses}}
- **Fix:** {{what changed, with file paths}}
- **Lesson:** {{the reusable rule that prevents recurrence}}
