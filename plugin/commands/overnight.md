---
description: Unattended boss-prompt mode — queue tasks, run headless bnb-loop laps, mandatory wake-up ritual.
argument-hint: <task-ids...>
---

You are setting up an **overnight** (unattended) run for: `$ARGUMENTS`. Use the
`bnb-overnight` skill — it defines the headless rules (never wait on the user;
`spiritual-guide.md` is the answer-of-last-resort), the `bnb-loop` launch commands, and
the mandatory wake-up ritual (full diff review + `bnb-verify` re-run before anything is
trusted).
