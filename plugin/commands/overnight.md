---
description: Unattended boss-prompt mode — queue tasks, run headless greenloop-loop laps, mandatory wake-up ritual.
argument-hint: <task-ids...>
---

You are setting up an **overnight** (unattended) run for: `$ARGUMENTS`. Use the
`greenloop-overnight` skill — it defines the headless rules (never wait on the user;
`spiritual-guide.md` is the answer-of-last-resort), the `greenloop-loop` launch commands, and
the mandatory wake-up ritual (full diff review + `greenloop-verify` re-run before anything is
trusted).
